## yourlib (JS/TS)

JS/TS package for the word datasets.

### Build and publish

```bash
cd js-lib
npm install
npm run build
npm publish
```

### Usage

```ts
import { listDatasets } from "yourlib";
console.log(listDatasets());
```
